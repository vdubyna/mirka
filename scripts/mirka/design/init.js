(function (define) {
  "use strict";

  define([
      "angular"
    ],
    function (angular) {
      angular.module.mirkaDesign = angular.module("mirkaDesign", ['ng']);

      return angular.module.mirkaDesign;
    });

})(window.define);